import { Typography, Link } from '@mui/material';
import React from 'react';

const CopyrightLink = () => {
    return (
        <Link color="inherit" href="https://deep-recommend.com/">
            DeepRecommend
        </Link>
    );
};

export default CopyrightLink;
